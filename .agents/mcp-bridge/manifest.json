{
  "name": "mcp-bridge",
  "version": "1.0.0",
  "runtime": "python",
  "entrypoint": "mcp_bridge.py",
  "description": "MCP protocol bridge — lets Claude/Cursor talk to the Brain cluster",
  "mcp_compatible": true,
  "tools": [
    {
      "name": "bridge_call",
      "description": "Route an MCP tool call through the hyper-brain network",
      "input_schema": { "tool": "string", "args": "object" }
    }
  ]
}
