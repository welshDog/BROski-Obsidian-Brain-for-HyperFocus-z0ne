# 🧠⚡ Style Settings Plugin — BROski Zone Setup

## Step 1 — Install the Plugin

1. Open Obsidian
2. `Settings` → `Community Plugins` → `Browse`
3. Search: **Style Settings**
4. Install + Enable ✅

---

## Step 2 — Add the CSS Snippet

1. `Settings` → `Appearance` → scroll to **CSS Snippets**
2. Click the **folder icon** to open the snippets folder
3. Copy `hyper-brain-themes.css` into that folder
4. Back in Obsidian — click **Reload** then toggle it ON ✅

---

## Step 3 — Activate BROski Zone

Once the snippet is active, open **Style Settings** (ribbon icon or `Ctrl+P` → "Style Settings").

You'll see the theme toggles. Enable:

```
☑ Hyperfocus BROski Zone
```

That adds `body.broski-zone` automatically. Done! 🐶💜

---

## Quick Class Reference

| Class | Mode |
|-------|------|
| `body.broski-zone` | 🧠 Hyperfocus BROski Zone |
| `body.focus-mode` | 🔥 Dark terminal focus |
| `body.calm-mode` | 🌙 Dyslexia-friendly warm |
| `body.hyper-mode` | ⚡ Ultra violet hyperfocus |

---

## Manual Override (no plugin needed)

If you prefer not to use Style Settings, paste this into any note's frontmatter:

```yaml
---
cssclass: broski-zone
---
```

That activates the theme for that note only. 🎯

---

*BROski Zone — built for ADHD + Dyslexia + Autistic brains. Every pixel has a reason.* 🧠⚡♾️
