{
  "name": "mcp-bridge",
  "version": "1.0.0",
  "runtime": "python",
  "entrypoint": "mcp_bridge.py",
  "description": "MCP bridge — connects BROski Brain to external MCP tool servers",
  "mcp_compatible": true,
  "cluster": "broski-brain",
  "port": 3302,
  "memory": "redis",
  "redis_db": 1,
  "tools": [
    {
      "name": "call_mcp_tool",
      "description": "Call any registered MCP tool via the bridge",
      "input_schema": { "tool_name": "string", "args": "object" }
    },
    {
      "name": "list_mcp_tools",
      "description": "List all available MCP tools registered to the bridge",
      "input_schema": {}
    }
  ]
}
